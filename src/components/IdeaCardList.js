import React from 'react';

const IdeaCardList = (props) => {
  return (
    <section className="card-section">
      {
      // TODO: map over Idea card here
      }
    </section>
  );
};

export default IdeaCardList;